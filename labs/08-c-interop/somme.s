// Labo 08 — somme(tab, n) appelée depuis C.

.global _somme
.align 2
.text

_somme:
    // TODO : x2 = 0 (accumulateur) ; boucle sur n éléments de 8 octets (ldr x3, [x0], #8)
    // TODO : x0 = x2 ; ret
    ret
